{

  "scripts": {
    "start": "json-server --watch db.json",
    "dev": "nodemon --watch db.json -e json --exec \"json-server --watch db.json\""
  },

  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
